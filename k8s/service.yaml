apiVersion: v1
kind: Service
metadata:
  name: eval-go-min
  labels:
    app.kubernetes.io/name: eval-go-min
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: eval-go-min
    app.kubernetes.io/managed-by: kubectl
spec:
  type: NodePort
  selector:
    app: eval-go-min
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
